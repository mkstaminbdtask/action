name: Cron Workflow

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  cron-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Cron Workflow
        run: |
          echo "Hello Cron Job!"
